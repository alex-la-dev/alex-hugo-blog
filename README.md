Requires installation of TailwildCSS and TailwindCSS CLI.
```bash
npm install --save-dev tailwindcss @tailwindcss/cli
```

Uses Hugo Function [css.TailwinCSS](https://gohugo.io/functions/css/tailwindcss/) to build the css file.

Run `hugo server -DF --noHTTPCache` to preview and test.
